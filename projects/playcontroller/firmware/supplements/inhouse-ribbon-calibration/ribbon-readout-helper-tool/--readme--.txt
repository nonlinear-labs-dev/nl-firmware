Ribbon Calibration Helper Tool for Linux-based host systems.
Reads out the raw AD-Converter values for the ribbon sensors.

Copy the content of this directory on an USB stick or make
it otherwise accessible for the host system.

Establish WiFi connection to the C15 on the host.

On the host, navigate to the directory and execute
the shell script via ./readout-ribbon-adcs.sh
