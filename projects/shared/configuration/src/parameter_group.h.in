#pragma once

/******************************************************************************/
/** @file       parameter_group.h
    @date       ${timestamp}
    @version    ${config.milestone}-${config.version}
    @author     M. Seeber
    @brief      provide parameter groups
    @todo       
*******************************************************************************/

#include "parameter_descriptor.h"
#include <vector>

namespace C15
{
    // descriptors of individual parameter groups
    constexpr ParameterGroupDescriptor ParameterGroups[] = {
        ${parameter_groups}
    };
    // elements of individual parameter groups
    ${get_parameter_ids}
} // namespace C15
