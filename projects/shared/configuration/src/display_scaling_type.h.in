#pragma once

/******************************************************************************/
/** @file       display_scaling_type.h
    @date       ${timestamp}
    @version    ${config.milestone}-${config.version}
    @author     M. Seeber
    @brief      provide display scaling types
    @todo       
*******************************************************************************/

#include "parameter_descriptor.h"

namespace C15
{
    constexpr const char * const ParameterUnits[] = {
        ${parameter_units}
    };
    constexpr DisplayScalingTypeDescriptor DisplayScalingTypes[] = {
        ${display_scaling_types}
    };
} // namespace C15
